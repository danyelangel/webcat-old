.renderWrapper(layout="column")
  wc-render-nav(
  tao="$ctrl.tao" 
  doc="$ctrl.doc")
  div(flex)
    wc-render-splash(
    metadata="$ctrl.doc.metadata")
    wc-render-sections(
    doc="$ctrl.doc")
  wc-render-footer(
  ng-if="$ctrl.doc.metadata.footer"
  metadata="$ctrl.doc.metadata")